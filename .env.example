# GitHub Configuration
# Repository where the runner will be registered
GITHUB_REPOSITORY=your_username/your_repository

# GitHub Personal Access Token (PAT)
# Required scopes: repo, workflow, admin:org (for org runners)
# Create at: https://github.com/settings/tokens
GITHUB_PAT=ghp_YOUR_TOKEN_HERE

# Runner Configuration
# Name for the runner (will appear in GitHub UI)
RUNNER_NAME=bananapi-f3-runner

# Working directory for the runner
RUNNER_WORKDIR=/home/poddingue/github-act-runner-test

# User account that will run the runner service
RUNNER_USER=poddingue

# Runner labels (comma-separated, these are automatic: self-hosted,linux,riscv64)
# Add custom labels if needed
RUNNER_LABELS=

# System Configuration
# IP address of the Banana Pi F3 (for Ansible)
BANANAPI_IP=192.168.1.185

# SSH user for Ansible connections
SSH_USER=poddingue

# SSH key path (optional, uses default if not set)
SSH_KEY_PATH=~/.ssh/id_rsa

# Optional: Docker Hub Configuration
# Only needed if you're pulling private images
DOCKERHUB_USERNAME=
DOCKERHUB_TOKEN=

# Optional: Build Configuration
# Go version to install (if building from source)
GO_VERSION=1.24.4

# PATH environment variable for the runner systemd service
# Override this if build tools are in non-standard locations
RUNNER_SERVICE_PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# GitHub Act Runner version (tag or branch)
RUNNER_VERSION=main

# Optional: Monitoring
# Enable Prometheus metrics export (true/false)
ENABLE_METRICS=false

# Metrics port
METRICS_PORT=9090

# Optional: Cleanup Configuration
# Enable automatic workspace cleanup (true/false)
AUTO_CLEANUP=true

# Disk space threshold for cleanup (percentage)
CLEANUP_THRESHOLD=80

# Optional: Auto-Update Configuration
# Enable weekly automatic updates for github-act-runner (true/false)
# When enabled, a cron job runs every Sunday at 3 AM to check for updates
RUNNER_AUTO_UPDATE=true

# Optional: Notification Configuration
# Telegram bot token for notifications
TELEGRAM_BOT_TOKEN=

# Telegram chat ID
TELEGRAM_CHAT_ID=
