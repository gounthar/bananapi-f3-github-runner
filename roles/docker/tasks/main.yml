---
# GPG key handling for Debian 12+ (apt-key is deprecated)
- name: Create keyrings directory
  file:
    path: /usr/share/keyrings
    state: directory
    mode: '0755'

- name: Download RISC-V64 Docker APT repository GPG key
  get_url:
    url: https://github.com/gounthar/docker-for-riscv64/releases/download/gpg-key/gpg-public-key.asc
    dest: /usr/share/keyrings/docker-riscv64-key.asc
    mode: '0644'

- name: Convert GPG key to binary format
  shell: gpg --dearmor < /usr/share/keyrings/docker-riscv64-key.asc > /usr/share/keyrings/docker-riscv64.gpg
  args:
    creates: /usr/share/keyrings/docker-riscv64.gpg

- name: Set permissions on GPG key
  file:
    path: /usr/share/keyrings/docker-riscv64.gpg
    mode: '0644'

- name: Remove old Docker APT repository (if exists with different key)
  file:
    path: /etc/apt/sources.list.d/docker-riscv64.list
    state: absent

- name: Remove old GPG key (if exists)
  file:
    path: /etc/apt/keyrings/docker-riscv64.asc
    state: absent

- name: Add RISC-V64 Docker APT repository
  apt_repository:
    repo: "deb [arch=riscv64 signed-by=/usr/share/keyrings/docker-riscv64.gpg] https://gounthar.github.io/docker-for-riscv64 trixie main"
    state: present
    filename: docker-riscv64
    update_cache: yes

- name: Install Docker packages from RISC-V64 repository
  apt:
    name:
      - docker.io
      - docker-cli
      - docker-compose
      - docker-buildx-plugin
      - containerd
      - runc
      - cagent
    state: present
    update_cache: yes

- name: Create Docker daemon configuration directory
  file:
    path: /etc/docker
    state: directory
    mode: '0755'

- name: Configure Docker daemon
  template:
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    mode: '0644'
  notify: restart docker

- name: Ensure Docker service is enabled and running
  systemd:
    name: docker
    enabled: yes
    state: started

- name: Add runner user to docker group
  user:
    name: "{{ runner_user }}"
    groups: docker
    append: yes

- name: Test Docker installation
  command: docker run --rm hello-world
  become_user: "{{ runner_user }}"
  changed_when: false
  register: docker_test
  failed_when: docker_test.rc != 0

- name: Clean up Docker test container
  command: docker system prune -f
  become_user: "{{ runner_user }}"
  changed_when: false
